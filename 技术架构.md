# AI 视频生产系统 · 技术流解析

> 生成时间：2026-09-09。本文件是整套系统的全景解析，改动系统前先看这里。

## 一、总体架构（三层）

┌─────────────────────────────────────────────┐
│  智能层：DSH Skill「comfyui-workflow-builder」 │  ← 白话文 → 工作流/优化/导入（cli.py 六子命令）
├─────────────────────────────────────────────┤
│  编排层：AI 导演台（web/app.py，端口 9081）      │  ← 脚本→分镜→逐镜生成→拼接；素材库；角色；标签
├─────────────────────────────────────────────┤
│  引擎层：ComfyUI 0.34.0（端口 8188）           │  ← H3 / Wan2.2 5B / Wan2.2 14B 三套管线
└─────────────────────────────────────────────┘
运行时：clora Python 3.10 + torch 2.11+cu130 · RTX 4060 Ti 16GB · 全本地、内网可迁移

## 二、核心数据流

### 主链路：一句提示词 → 成片

```
① 用户输入提示词
      ↓  本地 Ollama qwen3:8b（AI_SPLIT_SYS：专业分镜提示词；失败回退本地正则）
② 拆成输入分镜（每个镜头带 景别/运镜/时长 5·8·10·15/字幕）
      ↓  POST /api/split_states（AI_STATES_SYS）
③ 每个分镜再拆成若干「变化帧」= 连续动作与运镜中画面发生实质变化的关键状态
      ↓  相邻两帧 = 一段 H3 fl2va 首尾帧
④ 结合资产图出关键帧（POST /api/keyframe，Flux Kontext）
      ↓
⑤ 相邻两帧出一段视频（POST /api/segment）→ 整板出片（POST /api/board_film）合成成片
```

例：「拿起水杯喝水」→ 变化帧 = 远景桌上的水杯(固定) → 近景握住水杯的手(缓慢推近) → 面部特写(跟随镜头上移)。
每个变化帧在无限画布上是一个节点，连线标注这一段用的运镜与秒数。

### 资产图：文生图出底图，再图生图出六视图

```
角色/场景描述 → Ollama 译成 SDXL 英文提示词 → SDXL 出 4 张候选 → 选用 = 「底图」(character.image)
底图 → Flux Kontext 按六个机位各编辑一次（前/后/左/右/上/下）→ character.views（六视图资产）
```

六视图是**图生图**：只换机位，身份/脸/发型/服装/材质/光线全部保持不变（`ASSET_VIEW_RULE`），
输出保留底图原始画幅。关键帧生成时优先用**前视图 + 后视图**作角色参考（背对镜头的戏才有依据），
没有六视图就退回底图（`keyframe_references`）。

### 镜头生成的其余分支

故事版关键帧：分镜提示词 + 资产图 → 场景空景当编辑源、角色视图作附加参考 → Flux.1 Kontext dev 出该镜静帧
  → 缩到镜头分辨率 → 与「文生图定帧」共用同一套候选/选用 UI（ding_candidates + pick_ding → 首帧）
  ├─ 模型：MiniMax H3 / Wan2.2 5B / Wan2.2 14B（全局选择）
  ├─ 档位：极速/均衡/标准/高清（按模型映射步数）
  ├─ 素材注入：首帧图 / 多图参考(H3 ref2va 主体定义+BiRefNet 抠主体，自动匹配素材图也走这条) / 角色出场 / 配音文件
  └─ 提示词组装：画面 + 角色设定(注入) + 景别/运镜 + 负向词 + 风格后缀
→ build_graph 按模型生成 API 图 → 提交 ComfyUI /prompt → 轮询 /history
→ 视频落 output\video → 全部镜头完成 → ffmpeg 拼接 + 字幕叠加 → 成片
→ 同时把当前图导出到 Workflows 列表（按模型分文件）

## 三、三条模型管线对比

| 能力 | MiniMax H3 | Wan2.2 5B TI2V | Wan2.2 14B |
|---|---|---|---|
| 音频 | ✅ 原生音画（提示词画外音） | ❌ 无声（配音文件） | ❌ 无声（配音文件） |
| 帧率/5秒 | 24fps / 124 帧 | 16fps / 81 帧 | 16fps / 81 帧 |
| 参考图 | 首帧+末帧+**多图 ref2va 主体参考（抠主体）** | 单张首帧 | 单张首帧（I2V）/ 无（T2V） |
| 采样 | res_multistep+simple | uni_pc+simple | 双段：高噪 0→n/2 步 → 低噪 n/2→末步 |
| 档位步数 | 4/12/25/32 | 10/20/30/40 | 10/20/20/30 |
| 体积 | ~25GB（INT8+nvfp4） | ~19GB | ~57GB（4×fp8） |
| 定位 | 有配音的成片 | 快速出片 | 最高画质 |

> ⚠️ **2026-09-24 核实：Wan2.2 三条管线目前跑不起来。** `models\` 下 Wan2.2 相关的 10 个文件
> 全部是 **62 字节的占位文件**：`wan2.2_i2v_high/low_noise_14B_fp8_scaled`、
> `wan2.2_t2v_high/low_noise_14B_fp8_scaled`、`wan2.2_s2v_14B_fp8_scaled`、
> `wan2.2_ti2v_5B_fp16`、`umt5_xxl_fp8_e4m3fn_scaled`、`wan2.2_vae`、`wan_2.1_vae`、
> `wav2vec2_large_english_fp16`。Workflows 列表里那 4 个 Wan2.2 工作流一跑就报模型缺失。
> 所以本表右两列只是「设计目标」，**当前真实可用的只有 H3（fl2va int8 / ref2va fp8+int8 + turbo LoRA）、
> RealVisXL V5.0（SDXL，用于定帧与资产图）和 BiRefNet（抠主体）**。

## 三之二、图像侧模型（定帧与资产图）

| 用途 | 模型 | 入口 |
|---|---|---|
| 角色/场景设定图、文生图定帧 | RealVisXL V5.0（SDXL, 6.5GB）+ VAE | `/api/asset_image`、`/api/ding_frame` |
| 故事版关键帧（资产图参考编辑） | Flux.1 Kontext dev fp8（11.9GB）+ t5xxl_fp8（5.2GB）+ clip_l + ae | `/api/keyframe` |

Kontext 全线走 ComfyUI 核心节点（`ReferenceLatent` / `FluxKontextImageScale` / `FluxGuidance`），
**不需要任何第三方插件**；模型由根目录 `download_kontext.py` 从 ModelScope 断点续传下载（约 17.6GB，10.8MB/s 约 28 分钟）。
hf 直连不通，hf-mirror 对未缓存大文件会 308 跳回 huggingface，所以统一走 ModelScope。

## 四、导演台功能矩阵

- 输入层：脚本粘贴/docx 上传/AI 写剧本(DeepSeek,可选外网)、手动新增镜头、追加模式
- 增强层：首帧上传、多图参考(H3 ref2va 抠主体作主体定义)、角色固定(描述注入+参考图+角色出场)、配音文件上传、自动匹配素材图（命中素材抠主体作参考）
- 控制层：全局模型选择、速度档位、景别/运镜/时长/步数/种子/负向词、分辨率比例
- 数据层：自动保存(0.8s 防抖)、素材库（`assets\` 只收图片素材，上传/打标签，生成产物不入库）、素材检索
- 素材库存储：MySQL（事实/自增 id/使用信息）+ Neo4j（按 id 建 Asset-Tag-FITS 图，图算法检索）联合
- 素材检索（自动匹配素材图）：提示词 → jieba 分词（图词典对齐 Tag 词表）→ Neo4j 沿 Tag→Asset 检索计命中 + bge-m3 向量相似度语义兜底 → MySQL 查 id/路径 → 复制进 ComfyUI input/frames → BiRefNet 抠主体 → ref2va 主体参考（<Subject N>/<Picture N>）；Ollama 不可用时回退规则打分
- 输出层：逐镜预览、批量生成、拼接+字幕、工作流导出(按模型分文件)

### 故事板画布（无限画布，2026-09-24 新增）

分镜台顶部「分镜卡片 / 故事板画布」切换。画布把**每个镜头拆成若干「变化帧」**（连续动作与运镜中画面发生
实质变化的那几个关键状态），按「镜1帧1 → 镜1帧2 → … → 镜2帧1 → …」连成结构：

- **左侧是资产图节点**（角色设定图、场景设定图），每个镜头都从它们连出绿/青色的线——
  因为 `compose_prompt` 会把角色与场景描述注入每个镜头的提示词，关键帧也确实拿它们当参考图
- 节点 = 变化帧（画面描述 + 景别 + 关键帧图），连线 = 这一段的运镜与时长
- 资产图节点上可以直接「AI 生成底图」并选候选；再点「六视图（图生图）」用底图出前/后/左/右/上/下六张，
  节点上以 3×2 缩略图网格展示，产物写进 `character.views` / `scene.views`
- 相邻两帧就是一段 **H3 fl2va 首尾帧视频**（`first_frame`/`last_frame`），逐段生成、可单独重出
- 「整板出片」把缺的片段补全后按镜头顺序硬切合成成片（变化帧之间本来就是同一镜头的连续推进，不插过渡片）
- 交互：拖空白平移、滚轮缩放、拖卡片移动（位置存 `state.board`，纯 UI 布局数据）

例：「拿起水杯喝水」被拆成 远景桌上的水杯(固定) → 近景握住水杯的手(缓慢推近) → 面部特写(跟随镜头上移)，
三段时长按镜头总时长等比缩放，实测 9 秒目标拆出 3+3+3。

| 接口 | 作用 |
|---|---|
| `POST /api/split_states {scene_id, seconds}` | AI 把镜头拆成变化帧（本地 Ollama） |
| `POST /api/save_states {scene_id, states}` | 编辑变化帧内容（关键帧字段只能由选用接口写入） |
| `POST /api/keyframe {scene, state_index}` | 给某个变化帧出关键帧（Flux Kontext） |
| `POST /api/pick_state_keyframe {scene_id, state_index, file}` | 选定该变化帧的关键帧 |
| `POST /api/segment {scene_id, index}` | 相邻两帧 → 一段视频 |
| `POST /api/board_film` / `GET /api/board_film_status` | 整板出片（补全片段 + 合成） |
| `POST /api/board {positions}` | 保存画布布局 |

## 五、数据与持久化

| 数据 | 位置 |
|---|---|
| 镜头/角色/首帧/配音/参考图 | web\state.json（自动保存） |
| 工作流文件（14 个） | user\default\workflows\ |
| 模型（~111GB） | models\（diffusion 82GB、text_encoders 21GB、vae 7GB、loras 0.7GB） |
| 素材库（图片素材） | assets\（文件）+ MySQL comfyui_assets（自增 id/标签/使用信息/匹配流水）+ Neo4j Asset-Tag-FITS 图 + Ollama bge-m3（向量相似度） |
| skill 节点缓存 | .comfyflow\（object_info/models/catalog） |

## 六、Agent Skill（白话文接口）

comfyui-workflow-builder（全局安装）：
- 白话文 → 选节点 → API 图 → cli.py build 导入 Workflows 列表
- cli.py：discover / build / import / inspect / convert / submit
- 优化：inspect 看结构 → 按诉求改 → 重建导入
- 知识沉淀：references\workflow-format.md（环境事实+全部配方）

## 七、内网迁移体系

- migration\clora_env.tar.gz（5.89GB 环境包，torch+CUDA 冒烟验证通过）
- install_env.bat / start_comfyui.bat / start_director.bat（目标机一键）
- make_incremental.bat（代码增量包 ~30MB，withstate 参数带用户数据）
- 前端已全本地化（Tailwind/Iconify/Cytoscape 本地 vendor + Lucide 图标数据 lucide-icons.js），内网可用；改图标见 图标离线化说明.md
- 仅两个外网依赖：AI 写剧本(DeepSeek)、素材 AI 打标签(Qwen-VL)——均可手动/内网服务替代

## 八、技术栈明细

- 后端：Python 3.10 纯标准库（http.server 多线程），零第三方依赖
- 前端：原生 JS + Tailwind（本地编译脚本），无框架无构建
- 视频：ComfyUI 0.34.0（含 comfy_api/comfy_api_nodes 定制扩展、KJNodes）
- 拼接：imageio-ffmpeg 本地二进制 + drawtext 字幕
- 可选外网：DeepSeek（剧本）、Qwen-VL（素材标签）
- 本地数据库：MySQL 8.0（素材事实/使用信息，mysql.exe 零依赖直连）+ Neo4j（素材 id 知识图谱、图算法检索）
- 本地推理：Ollama（bge-m3 向量相似度；qwen3 系列已装但匹配流程不使用 LLM）+ jieba（分词，环境已含）
- 模型下载：ModelScope 主源 + huggingface 备用，断点续传+单实例锁+.part 保护

## 九、当前快照（2026-09-24）

- 状态：ComfyUI(8188) + 导播台(9081) 在线
- 工作流：H3 系列 + Wan2.2 系列（**Wan2.2 的会报模型缺失，见第三节警告**）
- 模型：H3 全套（fl2va int8 / ref2va fp8+int8 / turbo LoRA / audio vae / 32B 文本编码器）
  + RealVisXL V5.0（SDXL 定帧与资产图）+ BiRefNet；**Wan2.2 全套是 62 字节占位符，未下载**
- 本地文本/视觉推理：Ollama（qwen3:8b 拆镜与提示词、qwen3-vl:8b 可用于审图、bge-m3 向量匹配）
- 资产图链路：`/api/asset_image`（SDXL 出角色/场景设定图 4 张）+ `/api/pick_asset`（选用写回
  `character.image` / `scene.image`）；前端在「全局设置 → 角色固定/场景固定」里点「AI 生成设定图」
- 故事版关键帧链路：`/api/keyframe`（Flux Kontext 以资产图作参考出该镜静帧）；前端在镜头卡片上点「故事版关键帧（资产图参考）」
- 本地视觉自检：`qwen3-vl:8b` 可当审图员（本机无联网视觉服务），实测同一张图连问 5 次一致率 4/5，可用于「性别/年龄/发型」「几个人/是否全身入画」这类可判定问题
