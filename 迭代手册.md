# 迭代手册（下次回来怎么接上 / 怎么改）

> 适用：ComfyUI + 导演台 + DSH skill（comfyui-workflow-builder）这套系统的日常维护与迭代。
> 本文件放在 D:\app\comfyui 根目录，任何新会话先读这里。

## 一、恢复现场（新会话第一步）

1. 检查服务：ComfyUI(8188)、导播台(9081)、Ollama(11434，bge-m3 相似度用)
   - 没起就按「三、常用命令」里的启动命令后台拉起（日志重定向到文件）
   - Ollama 不在时自动匹配会回退为纯规则打分（功能不中断），起服务后自动恢复向量相似度
2. skill 已全局安装：comfyui-workflow-builder（C:\Users\Administrator\.agents\skills\）
   - 节点/模型缓存：D:\app\comfyui\.comfyflow\（object_info.json / models.json / catalog.md）
   - ComfyUI 重启或新装插件后：重新跑一次 cli.py discover 刷新缓存
3. 用户数据都在磁盘，刷新页面即恢复：
   - 镜头/角色/首帧：web\state.json
   - Workflows 列表：user\default\workflows\*.json
   - 成片/素材：output\video、output\image

## 二、日常改动流程（前端/后端/文档）

1. **前端**（web\static\app.js、index.html）：改完 node --check 验证语法，无需重启服务，
   浏览器 Ctrl+F5 即生效（静态文件带 no-store 缓存头）
2. **后端**（web\app.py）：
   a. clora 的 python -m py_compile web\app.py 验证语法
   b. **等 ComfyUI 队列空再重启**（正在生成时重启会打断当前镜头的完成回调）
   c. 杀 9081 监听进程，再重新后台启动
3. **验证**：先用独立小脚本测函数逻辑；**绝不要拿空列表/真实数据测写接口**
   （曾因此清空过 state.json 的镜头列表——教训）
4. **skill 沉淀**：新知识/新配方写 references\workflow-format.md；触发词/流程变化改 SKILL.md

## 三、常用命令速查

    # ComfyUI（日志重定向，避免 tqdm 写管道报错）
    D:\ProgramData\anaconda3\envs\clora\python.exe main.py --disable-pinned-memory --use-sage-attention > comfyui.log 2>&1

    # 导播台
    D:\ProgramData\anaconda3\envs\clora\python.exe web\app.py > director.log 2>&1

    # Ollama（bge-m3 向量相似度；端口被占说明已在运行）
    C:\Users\Administrator\AppData\Local\Programs\Ollama\ollama.exe serve

    # 重启导播台（杀 9081 监听进程）
    Get-NetTCPConnection -LocalPort 9081 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }

⚠️ 控制台「选择」模式会冻结服务：Windows 控制台处于 QuickEdit 选择模式（标题栏出现「选择」前缀）时，
进程写 stdout/stderr 会阻塞，整个服务卡死——表现就是点击「生成」后 ComfyUI 收不到命令、队列没反应。
按 Enter 可解除。启动全部.bat 现在把 ComfyUI 与导播台都以后台无窗口方式启动（ComfyUI 日志 comfyui.log / comfyui.out.log，导播台日志 director.log），
没有可点的控制台窗口，误关窗口与 QuickEdit 冻结问题已消除；手动前台启动服务后**不要点控制台窗口**，要看日志就 tail 日志文件。

生成单飞锁：导播台同一时刻只允许一个生成请求，重复点击/刷新后重提会立即提示「正在生成」，而不是在 ComfyUI 队列里静默排队；每次提交/完成/失败都会写进 director.log。

取消：先按 prompt_id 精确取消这次提交（在跑就中断、在排队就出队），再全局中断一次 ComfyUI 当前执行的任务——只精确取消不够，ComfyUI 在跑别的任务时（导播台重启过、过渡帧任务不占生成位）它会照样回 cancelled=true，GPU 继续跑到完，前端却显示已取消；中断没送到时前端会如实提示，不再假报成功。

    # 语法检查
    node --check web\static\app.js
    python -m py_compile web\app.py

## 四、Python 环境变更 → 重打包迁移包

- 依赖（pip/conda）发生变化后，重新打包：
  D:\ProgramData\anaconda3\Scripts\conda-pack.exe --ignore-missing-files -n clora -o migration\clora_env.tar.gz
  （--ignore-missing-files 用于跳过 pip 覆盖 conda 文件的历史遗留，环境运行正常时无害）
- 打包后建议按「解压 → torch+CUDA 冒烟测试」验证一次再交付

## 五、模型管理

- 下载模板：直连 huggingface + Range 断点续传（download_wan22_14b.py 可改 URL 列表复用；
  hf-mirror 对未缓存的大文件会 308 跳回 huggingface，故直连即可）
- 下完核对字节大小，ComfyUI 自动重扫（/models 接口可见即成功）
- 新模型接进导播台：app.py 加 MODELS_XXX + build_graph 分支；前端加模型选项 + 档位映射

## 六、当前系统全景（速记）

- 导播台功能：分镜解析(AI 拆镜三选：自动=本地 Ollama 优先 / 本地 Ollama / DeepSeek（设置里填 Key）；引擎不可用或输出不可解析时回退格式规则，不中断；上传的 .xlsx/.xls/.docx/Markdown 分镜表按行列逐行解析，一行一个镜头（时间/时长/画面/台词/字幕/旁白/音效列都认；无表头按内容猜列），表里内嵌的图片自动落 input/frames 挂成该镜首帧；旁白/配音列的原话会剥掉「（旁白）」这类标签进提示词、同时当底部字幕，占位符「（无旁白）」整格忽略；纯文字/故事才交给 AI 拆镜，已经是分镜表的不再让模型重拆——模型会把长表压成 8 个镜头) / 字幕渲染(白字黑边、字号 26 贴底居中、超 22 字折行，见 app.py SUB_FONT_SIZE/SUB_MARGIN/SUB_LINE_CHARS) / 追加模式 / 模型选择(H3、Wan2.2 5B、14B、S2V) / 速度档位(极速/均衡/标准/高清)
  / 首帧/多图参考上传 / 素材打标签(手动+AI) / 自动匹配素材图 / 角色固定+场景固定(均支持参考图：角色图→「角色出场」当首帧、场景图→「场景参考」作 ref2va 参考不占首帧；state 存 character.image / scene.image) / 自动保存 / 工作流导出(Director_最新镜头_H3 / _Wan22，按模型分文件)
  / 需要音频开关(H3 关闭后跳过原生音频与配音、Wan 跳过配音合入，成片无声但更快)
  / 上一镜尾帧续接开关(默认关：生成第 2 镜起自动抽上一镜成片的尾帧当本镜首帧，手动上传的首帧优先；/api/generate 传 chain_frames，app.py:chain_first_frame)
  / 字幕开关(合成时是否把镜头字幕烧录进画面) / 过渡补帧开关(默认开：合成时两镜之间插一段 H3 过渡片，关掉就是硬切；/api/concat 传 bridge=false)
  / 分镜台分面(一页一镜，可翻页)
  / 合成成片后弹窗预览成片(成片弹窗 + 下载)
  / 分镜时间线(分镜台顶部：画面/字幕双轨，按时长比例着色，点击跳转镜头) / 卡片时间码(成片起始时间)
  / 回到页面接回进度(刷新/返回首页/切项目后再进来：按 /api/progress 的 scene+gen_elapsed 把正在生成的镜头恢复成「生成中 + 实时采样进度」，后端不再占生成位时自动收掉徽标，不会一直显示生成中)
  / 控制台 app 壳(lg+：h-screen 满屏、两栏 grid-cols-2 等宽、面板内部滚动、底部严格对齐，墨刀 v3 对称布局；成片只走弹窗 finalModal，内联 finalPanel 已移除)
- 开始页(首页 /)：仿墨刀深色设计（logo + ComfyUI 状态 + 项目选择 + 新建项目），选项目后进入 /index.html 控制台
- 多项目：web\state.json=默认项目；新建项目存 web\projects\p时间戳_随机.json（{name, scenes, character}）；当前项目指针 web\current_project.json；接口 GET /api/projects、POST /api/project/new、POST /api/project/select；load_state/save_state 自动读写当前项目；控制台左上 logo 可返回首页，头部显示项目名徽标
- skill：白话文搭建/优化/导入，cli.py 子命令 discover/build/import/inspect/convert/submit
- 墨刀 AI MCP（前端设计迭代用，官方 Streamable HTTP）：
  - 端点 `https://modao.cc/agent-py/ai/mcp`，Header `modao-token: <个人令牌>`，令牌存 `web\.env` 的 MODOA_TOKEN
  - 本地客户端：`web\_mcp_http.py`（stdlib urllib 实现，list 列出工具 / call 调用并把完整结果存 `web\_mcp_last_result.json`）
  - 常用工具：generate_html / get_task_result / import_to_proto / continue_task / list_recent_tasks / get_account_status
  - 调用链：generate_html(需求) → get_task_result(轮询 task_id 到 completed) → import_to_proto(导入个人空间，多页任务不能传 page_name)
  - 生成结果含 content=HTML 正文 + preview_url；设计稿本地备份在 `html\ai-video-director_v2.html`
  - 注意：墨刀个人空间有文件数上限，import_to_proto 超限需清理空间或升级；生成消耗墨刀 AI 权益（积分）
- 迁移包：migration\（环境 tar.gz + 安装/启动脚本 + 迁移说明）
- 前端已全本地化（无 CDN 依赖），内网可用；仅 AI 写剧本/素材打标签依赖外网 API
- 素材库：`assets\` 目录只收图片素材（生成物不算素材）；MySQL `comfyui_assets` 存事实与使用信息（自增 id），Neo4j 按 id 建 Asset-Tag 图；凭据在 `web\.env`（MYSQL_\* / NEO4J_\*），Neo4j 密码 comfyui123
- 自动匹配素材图（`auto_match_image`）：jieba 分词（图词典=Tag 名+KG 关键词+素材标签词）→ Neo4j Tag→Asset 精确命中计分 → bge-m3（Ollama 11434，keep_alive=0 用完即卸）向量相似度兜底 → MySQL id → 复制进 ComfyUI input/frames → BiRefNet 抠主体 → ref2va 主体参考（<Subject N>/<Picture N>，不占首帧；本机 BiRefNet 权重只剩占位文件时自动跳过抠主体、直接用原图当参考）；Ollama 不可用回退规则打分，阈值/地址可用环境变量 BGE_COS_MIN(0.45)/OLLAMA_URL 调


- 专业分镜提示词（`web\app.py` 的 `AI_SPLIT_SYS`）：要求模型按「主体动作 / 角度构图 / 光线色调 / 环境道具 / 景深」写成一段连贯白描，
  并给出 shot（景别）/ camera（运镜）两个独立字段（枚举与前端 `SHOTS`/`CAMERAS` 一致，服务端 `SHOT_SIZES`/`CAMERA_MOVES` 兜底），
  时长只能 5/8/10/15。**切记不要在提示词里用「主体与外貌：」「光线：」这类带标签的分点模板**——实测 qwen3:8b 会把标签原样抄进画面描述，
  还会把同一段光线/环境复制到每个镜头，反而更差；改成连贯语句 + 一个范例后标签回声归零。
- 资产图（角色/场景设定图）：`POST /api/asset_image {asset_id, batch:4}` 用 SDXL 出 4 张候选
  （角色 768×1344 全身正面纯色背景，场景 1344×768 空景），`POST /api/pick_asset {asset_id, file}` 选用后写回这条资产的 `file`。
  描述取自画布上那张资产卡（名称/描述），先用 Ollama 译成 SDXL 英文提示词再加 `ASSET_SPECS` 的固定后缀。
  实测：角色图 4 张约 90 秒，场景图 4 张约 150 秒。**提示词的「画面框架」只写在后缀里，译文的 system 提示词别再重复一遍**，否则后缀会和译文叠成两句一样的构图描述。

- 故事版关键帧（④）：`POST /api/keyframe {scene, batch, steps}` 用 Flux.1 Kontext dev 出该镜静帧，前端在镜头卡片点「故事版关键帧（资产图参考）」。
  - 参考图顺序有讲究：**场景资产图放第一位当编辑源（它本来就是按镜头比例出的空景，画幅与成片一致），角色设定图放第二位当附加参考**。
    一开始把角色设定图（竖幅 768×1344 全身）当编辑源，Kontext 就在竖幅画布上采样，最后再裁成 16:9，
    结果只剩一个没有脸的半身——实测审图直接判「不是同一个人、不能当关键帧用」。
  - ⚠️ **同一镜的第 2 帧起，第一张参考图必须是「上一帧的关键帧」**（`keyframe_references(scene, st, prev)`）：
    相邻两帧本来就是同一个人的连续状态，从上一帧改，脸、衣服、背景都留在原地；每帧都从空景重画一遍，
    人脸每次都会重新长一个（实测：镜 2 的两帧完全不是同一张脸，用户一眼就看出来了）。
    这条走 `KONTEXT_SYS_CHAIN`（保留身份与背景，只改这一帧描述的变化），和「拿资产设定图建场景」的
    `KONTEXT_SYS_REF` 是两条指令，不能混用：混用会把设定图的影棚灰底或整个场景重建一遍，人跟着重画。
    注意**每一帧仍然把连线上的资产全拉一遍**（角色／设备／场景），上一帧只是排在第一位的编辑源，
    不是拿它顶掉资产参考——少了资产参考，跨镜头的一致性就断了。
  - 模型：`download_kontext.py` 从 ModelScope 拉 4 个文件（Kontext fp8 11.9GB + t5xxl_fp8 5.2GB + clip_l + ae，共 17.6GB）。
    下完**必须重启 ComfyUI** 才会重扫模型目录；验证方式：`/object_info/UNETLoader` 的 unet_name 里能看到 flux 文件名。
  - 耗时：4060 Ti 16GB 上 20 步、batch 2 约 275~360 秒（首次含 11.3GB 权重加载，之后快一些）。单张约 3 分钟，做长片故事版要有心理准备。

- 资产是**池子**（`state.assets` 列表），不是每类一个槽位：池子里有几条，画布最左一栏就画几张卡，
  「+ 资产」加一条（两个角色就加两条）。`kind` 决定类型，`name` 决定它连给谁。
  `ASSET_SPECS` 里 `equipment`（1:1 / 1024²，工业设备或产品正面照）/ `scene`（16:9 空景）/ `character`（9:16 全身）。
  加一类资产只要往 `ASSET_SPECS` 里加一条，画布资产栏、六视图、连线、`keyframe_references` 会自动带上。
  - 资产读写统一走 `POST /api/asset_info {asset_id?, kind?, name?, description?, image?}`：画布上的卡片按 `asset_id` 找，
    全局设置面板按 `kind` 找该类第一条（老项目迁移出来的 `character`/`scene`/`equipment` 三条，id 与类型同名）。
  - **自动连线**（`link_scene_assets`，每次 `load_state` 都重算一遍）：资产名字出现在镜头的
    `prompt`/`subtitle`/`environment` 里就连它；**没起名字的资产算这一类的默认资产，挂到每个镜头上**
    （老项目那个全局角色/场景就是这种），所以老片子的行为不变。
  - ⚠️ **主体字段（`subject`）不算匹配范围**：那里经常只是拿角色名给场景命名（「老陈的面馆」），
    照它连会把人物描述塞进一个空镜，定帧上就多出个人。地点名写在 `environment` 里，所以环境字段必须算。
  - 生成镜头时 `compose_prompt` 注入的是**这条连线上的**资产描述（「角色（全程固定同一人）：…」「场景（全程固定同一场景）：…」），
    不是一份全局角色——这正是「两个人物同片不串脸」的实现方式。手动排过引用的镜头打 `refs_manual`，
    自动连线不再覆盖它；`POST /api/link_assets {scene_id?}` 把它交回自动。
  - ⚠️ **别把资产种类写死成某几种**：`boardPosOf()` 里曾写成 `key === "@character" || key === "@scene"`，
    加「设备」后 `@equipment` 掉进兜底分支落到 (0,0)，和第一列镜头卡片**完全重叠**。现在按 `ASSET_KINDS` 判断。
  - ⚠️ **工作流文件名要净化**：`save_workflow_files()` 直接把传入名字当文件名，
    「设备/产品设定图」里的斜杠被当成目录，整个保存失败（`No such file or directory`）。现在用正则替换非法字符。

- 资产六视图（图生图）：`POST /api/asset_views {asset_id, views?, steps?, source?}`，状态 `GET /api/asset_views_status`（带 `asset_id`）。
  - 六视图对**设备和场景**最有用（相当于产品的六面图、场景的反打视角）；实测设备前视图判「同一台设备，外形/结构/颜色/面板位置均一致，完整无裁剪」，
    后视图判「设备背面视角，符合后视图资产需求」。
  - **是图生图，不是文生图**：底图取这条资产的 `file`（用户选的，或「AI 生成底图」出的那张），
    Kontext 以底图为编辑源，六个机位各跑一次，指令是 `ASSET_VIEWS` + `ASSET_VIEW_RULE`（只改机位，身份/脸/发型/服装/材质/光线全不变）。
  - 图里**不做最终 ImageScale**：保留参考图本身的画幅，避免把六视图裁坏（关键帧那条链路才需要裁到镜头分辨率）。
  - 每张约 1.5~2 分钟，六张约 10 分钟，做后台任务，中途失败保留已生成的机位。
  - 产物写这条资产的 `views = {front,back,left,right,top,bottom}`，画布资产卡上 3×2 网格展示。
  - `keyframe_references` 只看**这个镜头连到的资产**：场景空景当编辑源 → 设备前/后视图 → 角色前/后视图，**最多 4 张**（再多会互相稀释）；
    某类资产没有六视图就退回它的底图。「帧+资产生视频」也一样按连线给参考图（`shot_ref_images`），
    以前是把所有资产的六视图塞给每个镜头，两个人物的片子会把对方的设定图也当参考。

- 视频修补（真·掩膜重绘，Wan2.1-VACE + SAM3）：
  `POST /api/video_inpaint {scene_id, index?, target, prompt?, steps?, expand?, threshold?, seed?}`
  - 链路照官方 `video_wan_vace_inpainting` 模板接线：
    `CheckpointLoaderSimple(sam3.1_multiplex_fp16)` → `CLIPTextEncode(target)` → `SAM3_Detect(model, 帧, conditioning)`（按文字圈出目标）
    → `GrowMask(expand)` → `WanVaceToVideo(control_video=原帧, control_masks=掩膜)` → `KSampler(uni_pc, cfg=1, shift=5)` → `VAEDecode` → `CreateVideo(16fps)` → `SaveVideo`。
    VACE 只在掩膜里重画，掩膜外**一个像素都不动**——这是它和「换物体」的本质区别。
  - 权重：`download_video_inpaint.py`（约 42GB）。四样东西各自的位置很讲究：
    `diffusion_models/wan2.1_vace_14B_fp16`、`text_encoders/umt5_xxl_fp8_e4m3fn_scaled`、
    `vae/wan_2.1_vae`、`checkpoints/sam3.1_multiplex_fp16`。
    ⚠️ **SAM3 走的是 `CheckpointLoaderSimple`，必须放 `checkpoints/`**，放 diffusion_models 不认；
    而且官方要的是 `Comfy-Org/sam3.1` 那份（1.63GB），不是 `facebook/sam3`（3.2GB）。
  - ⚠️ **VACE 是 16fps 模型**：24fps 的成片直接喂进去会被当成 16fps，动作变慢。
    所以先用 ffmpeg `fps=16,scale=W:H` 抽成图片序列再喂 `LoadImagesFromFolderKJ`，出来再用 `CreateVideo(fps=16)` 合成，时长才对得上。
  - ⚠️ 单次最多约 **81 帧**（≈5 秒）：更长要先切短，或在画布上只修某一段片段。
  - VACE 出来的没声音，原片有音轨会用 ffmpeg 接回去。
  - SAM3 文本上限 32 token；多个目标用逗号分隔，`目标:数量` 限定个数（例如 `eye:2, window panels:4`）。
    ⚠️ **SAM3 吃的是英文**：用户的中文目标描述要先过 `SAM3_SYS` 压成简短英文名词短语再喂 `CLIPTextEncode`，
    否则中文提示圈不出东西。（VACE 那侧的 prompt 不用翻译，umt5 认中文。）
  - ⚠️ **`LoadImagesFromFolderKJ` 有两个坑**：
    一是 `image_load_cap` / `start_index` 在 `object_info` 里标着「可选」，但它的 Python 签名把它们当**必填位置参数**，
    不传直接 `TypeError`（我的静态校验器按 object_info 查，所以漏掉了这个——**object_info 的 optional 不等于真的可选**）；
    二是它的相对路径是拼到 KJNodes 自己的 `--base-directory`（没设就按**进程工作目录**找），
    传 `frames/xxx` 会 `FileNotFoundError`，**必须传绝对路径**。
  - 实测：3 秒片段（49 帧 @16fps、848×480）跑完 **820 秒（13.7 分钟）**，20 步约 36 秒/步。
    想提速就上官方模板用的 `Wan21_CausVid_14B_T2V_lora_rank32` Lightning LoRA（4 步出图），本机还没下。
  - 效果实测：把「透明玻璃杯」换成「白瓷茶杯」，**明显变化像素只占 0.05%**，视觉复核「其余画面没变、无接缝、成功」。
    对比「换物体」（整帧重画）——这才是真正的修补。

- 换物体（改已出成片里的某个东西）：
  `POST /api/edit_frames {scene_id, index?, instruction, steps?}` → 抽出成片的首帧（片段还抽尾帧）→
  Flux Kontext 按 `EDIT_FRAME_SYS` 改写成「只替换 X，其余一律不动」的指令逐帧编辑 → 候选存进 `slot.edit`；
  `POST /api/apply_edit {scene_id, index?, first, last?}` → 拿选中的帧当首（尾）帧重出这一段/这一镜。
  - **这是「改源再渲染」，不是视频修补**：画面干净不糊不闪。片段的首尾帧必须一起改——
    物体在视频里一直在动，只改首帧重出的话尾帧还是原样，物体会弹回去。
  - 重出会**沿用原片用过的 seed**（`states[i].seed`，`/api/segment` 生成时记下来的），运动能尽量对上。
  - 原片不删：存进 `clip_prev`（镜头级是 `output_prev`），想回退直接换回来。
  - **要和「视频修补」区分开**：换物体是「改关键帧再整段重渲染」，代价小但整段画面都是新生成的；
    视频修补是「只在掩膜里重画」，掩膜外原样保留，代价大但其余像素完全不动。前者适合改构图/换主角，后者适合抠掉穿帮的人或换掉一个物件。

- 版本分叉 + 宫格（参照 LibTV 的节点工作流）：
  - **每次生成不再覆盖，而是在父节点下面挂一个「宫格衍生节点」**（绿色虚线连回父节点），多个批次并存。
    数据在 `<field>_batches`（每次生成一批，带 `n` 序号做稳定 key）和 `<field>_versions`（扁平版本清单）里；
    `keyframe` / `clip` 仍然只表示「当前选中的那一版」。生成时关键帧一次出 4 张 = 一个 2×2 宫格。
  - 点宫格里任意一格 = **宫格切分 + 设为当前版本**（`POST /api/set_version {scene_id, index?, field, rel}`，
    field 是 `keyframe` 或 `clip`）；卡片下方有「版本 N / M」和 ‹ › 可以来回切。原片不丢。
  - ⚠️ `save_states` 的白名单必须带上 `keyframe_versions/keyframe_batches/clip_versions/clip_batches/clip_prev/seed/edit/inpaint`，
    否则在画布上编辑一次变化帧就把版本历史冲掉了。
  - ⚠️ **`boardNodeSig` 里踩过运算符优先级的坑**：`(x ? x.candidates : s.ding_candidates || []).join()`
    在「x 存在但 x.candidates 未定义」时会 `undefined.join()` 抛错，**整个画布一个节点都渲染不出来**（白屏）。
    正确写法是 `(x ? (x.candidates || []) : (s.ding_candidates || []))`。这个 bug 原来就埋着，是新的数据结构把它引爆的。
  - ⚠️ **加节点类型时，`renderBoard` 自己的节点列表要跟着改**：它一度是手写拼 `boardAssetNodes()+boardHeaderNodes()+boardNodesOf()`，
    加了宫格节点后 `boardAllNodes()` 有了、`renderBoard` 却还在用手写列表，结果宫格一个都不显示。现在统一走 `boardAllNodes()`。
  - 列内高度会随版本累积变化：`boardColumnY()` 把每个节点下面的宫格高度也算进去，下面的变化帧自动让位。

- 故事板画布（无限画布）：分镜台顶部可切「分镜卡片 / 故事板画布」。画布节点是**变化帧**，连线是这一段的运镜与时长；
  相邻两帧 = 一段 H3 fl2va 首尾帧视频。
  - 拆变化帧：`/api/split_states`，提示词是 `web\app.py` 的 `AI_STATES_SYS`。**运镜方向必须由提示词强制**——
    早期版本用户写「逐渐推进至近景手」，模型却给了「缓慢拉远」，加了「用户说推进就用缓慢推近，说拉远才用缓慢拉远」才纠正。
  - 时长不要在提示词里指望模型算对：`/api/split_states` 拿到镜头总时长后，对前 N-1 帧的 seconds **按比例等比缩放**（夹在 3-15），
    这样「片段总时长」必然等于镜头时长。
  - `seconds` 的语义是「从这一帧推进到下一帧的时长」；**最后一帧的 seconds 不参与生成**（它没有下一帧）。
  - 画布布局（拖拽后的 x/y）存在项目 state 的 `board` 键下，与镜头数据解耦：`POST /api/board`。
  - 前端画布是纯原生 JS（CSS transform + 绝对定位 + SVG 连线），没引任何图形库；Tailwind 是本地 Play CDN，新类名直接可用。
  - ⚠️ `OUTPUT_DIR` 是 `<root>\output\video`，所以镜头成片与片段路径**已经是相对 output\video 的**，
    前端拼 `/output/<路径>` 即可，**不要自己再加 "video/" 前缀**（加过一次，播放 404）。

## 七、更新代码后迁移到内网（增量包）

- 跑 `migration\make_incremental.ps1`（计划任务 ComfyUI-Incremental-Package 每天 12:00 也会自动打一份）
- 只想带「更新部分」（代码/前端/文档/工作流，约 150KB，不含项目数据）：跑 `migration\make_update.ps1` → `migration\update_时间戳.zip`
  - 默认：代码/前端/工作流/文档（约 30MB），不含用户数据
  - 加参数 `withstate`：连 `web\state.json`（镜头/角色）一起打包
- 产物：`migration\incremental_时间戳.zip`
- 除模型外的两份定位包（与上面两个增量包并存，各自附 `*_清单.txt` 与 `logs\*.log`）：
  - `migration\make_code_package.ps1` → `migration\code_时间戳.zip`（约 26MB）：全部代码/前端/文档/脚本/工作流，不含 models、运行环境、工具、用户数据（含 `web\state.json`），解压覆盖 `D:\app\comfyui`
  - `migration\make_tools_package.ps1` → `migration\tools_时间戳.zip`（约 800MB）：便携 MySQL8 + Neo4j（含 JDK21）+ 便携 Chrome，不含各库 `data\` 实时数据，解压覆盖 `D:\app\`（得到 comfyui 与 tool 两棵树）
- 内网机操作：解压覆盖 `D:\app\comfyui` → 重启 ComfyUI/导播台即可
- ⚠️ 覆盖会连 `web\app.py` 一起回退：别拿旧日期的增量包往新代码上盖（今天出现过 app.py 被退回旧版、多项目/拆镜功能消失）；盖完先 `python -m py_compile web\app.py`，再打开导播台确认项目列表与解析分镜正常。
- **只有 pip/conda 依赖变化时才需要重打 `clora_env.tar.gz`**（见第四节）

## 八、改完怎么自检（别拿真实项目乱试）

1. **前端**：`node --check web\static\app.js`；**后端**：`python -m py_compile web\app.py`（都用 clora 的 python）。
   Tailwind 是本地 Play CDN（`web\static\vendor\tailwind.js`，运行时 JIT），新写的原子类不用重新编译，浏览器 Ctrl+F5 即生效。
2. **要测写接口，先建一次性项目**，别在真实项目上试：
   - 写 `web\projects\pTEST_xxx.json`（`{name, scenes:[], character:{...}, scene:{...}}`）
   - 备份并把 `web\current_project.json` 指向它 → 测 → **用完把指针复制回去并删掉测试项目**
3. ⚠️ **起 ComfyUI 一定要脱离本会话、并且把 stdout/stderr 重定向到真实文件**。用工具自带的后台任务（管道）去起，
   tqdm 写进度条时会 `OSError: [Errno 22] Invalid argument`（`app/logger.py` 的 flush 失败），采样直接中断，
   表现为「KSampler 报 Errno 22」。正确做法和 `启动全部.bat` 一致：
   `Start-Process cmd.exe -ArgumentList '/c','"<clora python>" main.py --disable-pinned-memory --use-sage-attention > comfyui.log 2>&1' -WindowStyle Hidden`。

4. ⚠️ **改 `web\current_project.json` 千万别用 PowerShell 的 `Set-Content -Encoding UTF8`**——Windows PowerShell 会写成 **带 BOM**，
   `json.load` 直接抛异常，`_current_project_id()` 静默退回 `"default"`，于是你以为在测测试项目、其实读到的是 `web\state.json`。
   用编辑器的「UTF-8 无 BOM」保存，或者干脆让导播台自己的接口去改。改完用
   `python -c "print(open(r'web\current_project.json','rb').read())"` 确认开头是 `b'{"` 而不是 `b'\xef\xbb\xbf{"`。
4. **验证生成结果不用人眼**：本机 `qwen3-vl:8b` 可以当审图员。把图 base64 塞进 `/api/chat` 的 `images` 字段，
   问「画面里有几个人 / 是否全身入画 / 是否正面 / 背景是否干净」这类可判定的问题，比肉眼看缩略图更可靠（实测 4 张角色设定图 3 张完全达标）。
5. **跑本地模型测效果时把结果写进 UTF-8 文件再用编辑器看**：PowerShell 控制台是 GBK，中文直接 print 出来全是乱码，容易误判。
6. **用 qwen3-vl:8b 审图前，先验一下它稳不稳**：同一张图问同一个问题 5 次，看答案一致率。
   它给长篇「像不像同一个人」的结论时前后会自相矛盾（同一张角色图，第一次说「银灰色短发」、第二次说「女性、圆脸、偏年轻」），
   但问「性别/年龄段/发型」这种短答案时 5 次里 4 次一致，可以信。**结论：只问可判定的短问题，不要采信它的长篇评语。**
7. **AI 出图提示词里必须显式写性别**：角色描述「五十多岁面馆老板老陈、国字脸、花白短发」没提性别，
   SDXL 直接画成了女性（VL 5 次里 4 次判「女/50-60岁」）。给 `ASSET_SPECS["character"]["sys"]` 加上
   「必须先用 male/female 写明性别，没直说时按姓名身份合理判断」之后，4 张候选全部判为「男/50-60岁/短发」。
   同理，Kontext 的译文提示词也加了「性别、年龄、发型发色、服装颜色必须原样写进指令，不要概括成 elderly man 这类笼统说法」。
